import {getProjects} from '../lib/store';import {Link} from 'react-router-dom';
export default function Projects(){const x=getProjects().slice().reverse();return <><div className="pageTitle"><p className="eyebrow">HISTORY</p><h1>Projects</h1><p>Every generated campaign stays organized here.</p></div>{x.length?<div className="grid cards">{x.map(p=><Link to={'/results/'+p.id} className="projectCard" key={p.id}><img src={p.outputs[0]}/><div><b>{p.productName}</b><span>{p.style} · {p.space} · {p.mood}</span></div></Link>)}</div>:<div className="empty">No generation history yet.</div>}</>}
